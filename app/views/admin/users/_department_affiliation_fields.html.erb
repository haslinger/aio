<div class="nested-fields">
  <%= f.input :department_id, :required => false do %>
    <%= f.input_field :department_id, :collection => Department.where(:company_id => current_company.id) %>
    <%= link_to_remove_association t("helpers.remove_department_affiliation"), f, :class => "btn btn-small btn-danger" %>
  <% end %>
  <%= f.hidden_field :company_id, value: current_company.id %>
</div>
