<%- model_class = Currency -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:company_id) %>:</strong></dt>
  <dd><%= @currency.company.name %></dd>
  <dt><strong><%= model_class.human_attribute_name(:currency) %>:</strong></dt>
  <dd><%= @currency.currency %></dd>
  <dt><strong><%= model_class.human_attribute_name(:shorttext) %>:</strong></dt>
  <dd><%= @currency.shorttext %></dd>
  <dt><strong><%= model_class.human_attribute_name(:created_by) %>:</strong></dt>
  <dd><%= @currency.created_by %></dd>
</dl>
<%= versioning_helper(@currency) %>
<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              currencies_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_currency_path(@currency), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              currency_path(@currency),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
