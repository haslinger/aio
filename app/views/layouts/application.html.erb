<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "AIO" %></title>
    <%= csrf_meta_tags %>
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= render "admin/favicon" %>
  </head>
  <body>

    <%= render "admin/navbar" %>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span3">
          <div class="well sidebar-nav">
            <%= render "sidebars/" + session[:menu] %>
          </div><!--/.well -->
          <%= domain_helper %>
        </div><!--/span-->
        <div class="span9">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->

    <%= render "admin/footer" %>
    </div> <!-- /container -->

    <!-- Javascripts -->
    <%= javascript_include_tag "application" %>
    <%= yield :javascript %>
  </body>
</html>
