<%- model_class = InvoicePosition -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human(:count => 2) %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:invoice_header_id) %></th>
      <th><%= model_class.human_attribute_name(:textposition) %></th>
      <th><%= model_class.human_attribute_name(:product_id) %></th>
      <th><%= model_class.human_attribute_name(:quantity) %></th>
      <th><%= model_class.human_attribute_name(:unit_id) %></th>
      <th><%= model_class.human_attribute_name(:discount) %></th>
      <th><%= model_class.human_attribute_name(:value) %></th>
      <th><%= model_class.human_attribute_name(:vatrate) %></th>
      <th><%= model_class.human_attribute_name(:price) %></th>
      <th><%= model_class.human_attribute_name(:base_price) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:created_by) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @invoice_positions.each do |invoice_position| %>
      <tr>
        <td><%= link_to invoice_position.id, invoice_position_path(invoice_position) %></td>
        <td><%= invoice_position.invoice_header_id %></td>
        <td><%= invoice_position.textposition %></td>
        <td><%= invoice_position.product_id %></td>
        <td><%= invoice_position.quantity %></td>
        <td><%= invoice_position.unit_id %></td>
        <td><%= invoice_position.discount %></td>
        <td><%= invoice_position.value %></td>
        <td><%= invoice_position.vatrate %></td>
        <td><%= invoice_position.price %></td>
        <td><%= invoice_position.base_price %></td>
        <td><%= invoice_position.description %></td>
        <td><%= invoice_position.created_by %></td>
        <td><%=l invoice_position.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_invoice_position_path(invoice_position), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      invoice_position_path(invoice_position),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new"), :model => model_class.model_name.human),
            new_invoice_position_path,
            :class => 'btn btn-primary' %>
